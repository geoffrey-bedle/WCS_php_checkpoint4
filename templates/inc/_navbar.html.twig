<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('home') }}"><img class="logo-circus" src="/assets/img/wildcircuslogo.png"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item {% if app.request.server.get('PATH_INFO') == path('tour_list') %}active{% endif %}">
                <a class="nav-link" href="{{ path('tour_list') }}">Les spectacles<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item {% if app.request.server.get('PATH_INFO') == path('galery_categories') %}active{% endif %}">
                <a class="nav-link" href="{{ path('galery_categories') }}">Galerie</a>
            </li>
            <li class="nav-item {% if app.request.server.get('PATH_INFO') == path('infos') %}active{% endif %}">
                <a class="nav-link" href="{{ path('infos') }}">Informations</a>
            </li>
            <li class="nav-item {% if app.request.server.get('PATH_INFO') == path('contact') %}active{% endif %}">
                <a class="nav-link" href="{{ path('contact') }}">Contact</a>
            </li>
        </ul>
    </div>

</nav>